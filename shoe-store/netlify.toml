[build]
  command = "npm run build:client"
  publish = "dist/spa"

# SPA fallback — all routes serve index.html (React Router handles them)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
